[build]
# Enforce clippy rules from .clippy.toml — never allow warnings.
rustflags = ["-D", "warnings"]

[profile.dev]
debug = 0
opt-level = 0

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"